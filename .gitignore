# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test artifacts
coverage
.nyc_output
playwright-report
test-results
dist-test

# Environment files
.env
.env.local
.env.*.local
# vite.config.ts calls loadEnv with an empty prefix, so it reads UNPREFIXED
# server secrets (GEMINI_API_KEY, STRIPE_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY)
# out of .env.production and .env.development too. Ignore every .env variant
# and re-admit only the checked-in template.
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase export output — review, then move desired files into courseData/
courseData/exported/
