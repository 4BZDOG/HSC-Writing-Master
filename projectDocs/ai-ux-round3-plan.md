# AI UX Round 3 Plan

Third review round (read-only agent findings). Same conventions: British/Australian English, design tokens, mock `services/geminiService.ts` in tests, `npm run test:all` before each commit, band/Verb-Gate logic untouched.

| ID  | file:line                                        | Category              | Severity | Description                                                                                                                                                                                        |
| --- | ------------------------------------------------ | --------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | `components/QualityCheckModal.tsx:52-61,152-155` | reliability           | **high** | `runCheck` never clears `error`; the body renders `error` before `result`, so a retry that _succeeds_ still shows the error card — the user is stuck until they reopen.                            |
| F2  | `components/QualityCheckModal.tsx:253`           | consistency/microcopy | med      | Footer hardcodes "Review generated by Gemini 2.5 Pro"; the check routes through the `reasoning` role, which the admin engine selector can point at Gemini 3 Pro or Claude Sonnet. Stale and wrong. |
| F3  | `components/WorkspaceRightPanel.tsx:423-428`     | clarity/microcopy     | med      | On an evaluation timeout, students/teachers are told to "switch to Gemini Flash in the AI Engine selector" — an admin-only control they cannot see.                                                |
| F4  | `components/QualityCheckModal.tsx:36,94`         | consistency/a11y      | low      | Escape and backdrop close the modal mid-check, unlike sibling AI modals which guard close while a request is in flight.                                                                            |
| F5  | `components/DotPointGeneratorModal.tsx:141-146`  | consistency           | low      | Footer "Cancel" isn't `disabled` during generation (silently no-ops), unlike sibling modals.                                                                                                       |

## Steps

1. **QualityCheckModal (F1, F2, F4):** reset `error`/`result` at the top of `runCheck` + a re-entrancy ref so a successful retry shows the result; gate Escape + backdrop on `!isLoading`; replace the hardcoded attribution with the live `reasoning` engine label (`getModelById(getSelectionSnapshot()['reasoning'])?.label`, read in render, not module scope). Add `tests/unit/qualityCheckModalRetry.test.tsx` (mock `performQualityCheck` reject-then-resolve).
2. **WorkspaceRightPanel (F3):** replace the admin-only timeout tip with advice any user can act on (retry shortly / shorten the response); keep the Try Again button.
3. **DotPointGeneratorModal (F5):** add `disabled={isLoading}` to the footer Cancel to match sibling modals.

## Verification & review

Run `/code-review` on the diff, `npm run test:all`, then PR + merge when green.
